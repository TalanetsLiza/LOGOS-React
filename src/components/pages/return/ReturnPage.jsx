import styles from "./ReturnPage.module.scss";

const ReturnPage = () => {
    return (
        <div className={styles.container}>
            ReturnPage
        </div>
    );
};

export default ReturnPage;
